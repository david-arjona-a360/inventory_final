# Devlog — 2026-07-09

## Finished: All 3 Phases of Inventory System Overhaul

### Phase 1 — Bug Fixes
- `auth_service.py:12`: `os.sys.path` → `sys.path` (AttributeError)
- `auth_manager.py:122`: bare `except:` → `except Exception:` + traceback
- `equipos/config.py:57`: bare `except:` → `except Exception:` + logging
- `inventory_view.py`: QTimer leak fixed (`deleteLater()`, parented timer)

### Phase 2 — Architecture Refactor
- Created `equipos/inventory_app_qt/config/settings.py` (self-contained settings)
- Created `equipos/inventory_app_qt/services/excel_service.py` (ExcelService class)
- Updated all import paths across 6+ files to use modular `config.settings.*`
- Fixed import conflict: `config` resolving to legacy `equipos/config.py` instead of `inventory_app_qt/config/` package
- Added Windows SSO type radio buttons in user management dialogs

### Phase 3 — Aesthetic Modernization
- **`Theme/qss.py`**: Shared `build_stylesheet()` with 6px border-radius, hover/focus, scrollbar, header, table styling
- **`Theme/icons.py`**: 9 SVG icon generators (`icon_add`, `icon_edit`, `icon_delete`, `icon_refresh`, `icon_logout`, `icon_users`, `icon_search`, `icon_report`, `icon_inventory`)
- **Login dialogs**: Card-style with `QGraphicsDropShadowEffect`, 12px rounded corners
- **All buttons** across both apps now use SVG icons (nav, inventory toolbar, item form, user management)
- **Both `main.py`** use shared QSS builder (removed inline duplicates)

### Verifications
- `python launcher.py --app-equipos-qt` — starts clean
- `python launcher.py --app-insumos` — starts clean
- All 30+ module imports verified
- CHANGELOG.md updated with full entries for all 3 phases
